% \iffalse meta-comment
% Copyright (C) 2026 Russell Wright Helder
% \fi
%
% \iffalse
%<*driver>
\ProvidesFile{rhelder3.dtx}[2026/07/18 v0.1.0 Personal document class]
\documentclass{l3doc}

\usepackage{biblatex}
\usepackage{xfrac}

\addbibresource{rhelder3.bib}

\RenewDocElement[
  envlike,
  idxgroup = Package options,
  idxtype = opt.,
  printtype = \textit{opt.},
]{Option}{optionenv}

\NewDocElement[
  macrolike,
  idxgroup = Package commands,
  idxtype =,
]{InterfaceMacro}{imacro}

\NewDocElement[
  macrolike,
  idxgroup = Internal LaTeX macros,
  idxtype =,
]{InternalMacro}{lmacro}

\NewDocElement[
  macrolike,
  idxgroup = LaTeX length (dimen),
  idxtype = dimen,
  printtype = \textit{dimen},
]{LaTeXDimen}{ldimen}

\NewDocElement[
  macrolike,
  idxgroup = LaTeX length (skip),
  idxtype = skip,
  printtype = \textit{skip},
]{LaTeXSkip}{lskip}

\NewDocElement[
  envlike,
  idxgroup = LaTeX counter,
  idxtype = counter,
  printtype = \textit{counter}
]{LaTeXCounter}{lcounter}

\NewDocumentCommand{\acro}{m}{\textsc{\MakeLowercase{#1}}}
\NewDocumentCommand{\bool}{m}{\texttt{#1}}
\NewDocumentCommand{\hook}{m}{\texttt{#1}}
\NewDocumentCommand{\num}{m}{#1}
\NewDocumentCommand{\opt}{m}{\texttt{#1}}
\NewDocumentCommand{\unit}{m}{\texttt{#1}}

\begin{document}
  \DocInput{rhelder3.dtx}
\end{document}
%</driver>
% \fi
%
% \GetFileInfo{rhelder3.dtx}
%
% \title{The \cls{rhelder3} Class}
% \author{Russell Wright Helder}
% \date{\filedate \quad \fileversion}
%
% \maketitle
%
% \begin{implementation}
%
% \section{Identification}
%
% Define a prefix for internal commands (\pkg{docstrip} will replace
% \texttt{@@} with this prefix when generating the class file).
%    \begin{macrocode}
%<@@=rh>
%    \end{macrocode}
% Check that we are running on top of \LaTeXe{}
% and are using a recent version of the kernel
% that contains the L3 programming layer,
% \pkg{xparse},
% and the command \cs{ProcessKeyOptions}.
%    \begin{macrocode}
\NeedsTeXFormat{LaTeX2e}[2022-06-01]
%    \end{macrocode}
% Identify the class file and enable \pkg{expl3} syntax.
%    \begin{macrocode}
\ProvidesExplClass{rhelder3}{2026/07/18}{0.1.0}{Personal document class}
%    \end{macrocode}

% \section{Options}
%
% \begin{optionenv}{showgrid}
%     Boolean (default \bool{false}).
%     Show baseline grid. Useful for document design.
%    \begin{macrocode}
\keys_define:nn {rh}{
  showgrid .code:n =
    \RequirePackage{tikz}

    \dim_new:N \l_@@_to_first_baseline
    \dim_new:N \l_@@_yshift

    \hook_gput_code:nnn {begindocument}{.}{\@@_show_baseline_grid:},
}

\ProcessKeyOptions[rh]
%    \end{macrocode}
% \end{optionenv}

% \section{Horizontal Motion}
%
% \subsection{Measure}
%
% \begin{imacro}{\SetTextwidth}
%    \marg{integer}\\
%    Sets \cs{textwidth}
%    so that the line will contain
%    an average of roughly \meta{integer} characters.
%    This formula for calculating the line width
%    is extracted from the copyfitting table at
%    \autocite[28]{bringhurst1992ElementsTypographic}.
%    \begin{macrocode}
\NewDocumentCommand{\SetTextwidth}{m}{\@@_set_textwidth:n {#1}}

\dim_new:N \l_@@_fontwidth

\cs_new:Npn \@@_set_textwidth:n #1 {
  \hbox_set:Nn \l_tmpa_box {abcdefghijklmnopqrstuvwxyz}
  \dim_set:Nn \l_@@_fontwidth {\box_wd:N \l_tmpa_box}
  \geometry{
    textwidth=\dim_eval:n {
      (0.0325\l_@@_fontwidth + 0.378pt) * #1
    }
  }
}
%    \end{macrocode}
% \end{imacro}

% \section{Leading}
%
% \begin{variable}{
%   \g_@@_normal_size,
%   \g_@@_normal_leading,
%   \g_@@_large_size,
%   \g_@@_large_leading,
% }
%    Dimensions to be used by size-changing commands like \cs{normalsize}.
%    Will be set to \cs{baselineskip} by \cs{normalsize}.
%    \begin{macrocode}
\dim_new:N \g_@@_normal_size
\dim_new:N \g_@@_normal_leading
\dim_new:N \g_@@_large_size
\dim_new:N \g_@@_large_leading

\dim_set:Nn \g_@@_normal_size {12pt}
\dim_set:Nn \g_@@_normal_leading {15pt}
\dim_set:Nn \g_@@_large_size {14pt}
\dim_set:Nn \g_@@_large_leading {18pt}
%    \end{macrocode}
% \end{variable}

% \begin{imacro}{\normalsize}
%    Sets the font size and leading.
%    Also sets \cs{parskip} equal to one lead,
%    and sets generic list parameters.
%    \begin{macrocode}
\RenewDocumentCommand{\normalsize}{}{
  \@setfontsize\normalsize{\g_@@_normal_size}{\g_@@_normal_leading}
  \dim_set_eq:NN \parindent \baselineskip
  \let\@listi\@listI
}
%    \end{macrocode}
% \end{imacro}

% \begin{imacro}{\large}
%    Set other font sizes and leadings.
%    \begin{macrocode}
\NewDocumentCommand{\large}{}{
  \@setfontsize\large{\g_@@_large_size}{\g_@@_large_leading}
}
%    \end{macrocode}
% \end{imacro}

% Initialize font size.
%    \begin{macrocode}
\normalsize
%    \end{macrocode}
%
% \begin{lskip}{\topskip}
%    Set leading for the first line on the page.
%    \begin{macrocode}
\skip_set_eq:NN \topskip \g_@@_normal_size
%    \end{macrocode}
% \end{lskip}

% \section{Paragraphs}
%
% \begin{ldimen}{\parindent}
%    Initialize \cs{parindent} to \cs{baselineskip},
%    although this is not strictly necessary
%    (\cs{parindent} will be set to \cs{baselineskip}
%    when \cs{normalsize} is called).
%    \begin{macrocode}
\dim_set_eq:NN \parindent \baselineskip
%    \end{macrocode}
% \end{ldimen}
%
% \begin{lskip}{\parskip}
%    Don't put any stretchable space between paragraphs,
%    so that the text snaps to baseline.
%    \begin{macrocode}
\skip_set:Nn \parskip {0pt}
%    \end{macrocode}
% \end{lskip}

% \subsection{Baseline Grid}
%
% \begin{macro}{\@@_show_baseline_grid:}
%    Typesets baseline grid.
%    Called if \opt{showgrid} is set.
%    \begin{macrocode}
\cs_new:Npn \@@_show_baseline_grid: {
  \dim_set:Nn \l_@@_to_first_baseline {\Gm@tmargin + \topskip}
  \dim_set:Nn \l_@@_yshift {
    \fp_eval:n {
      \fp_to_int:n {\l_@@_to_first_baseline / \g_@@_normal_leading} -
      \l_@@_to_first_baseline / \g_@@_normal_leading
    }\g_@@_normal_leading
  }

  \hook_gput_code:nnn {shipout/background}{.}{
    \put (0, 0){
      \begin{tikzpicture}[remember~picture, overlay]
        \draw [ystep=\g_@@_normal_leading, xstep=0, help~lines]
          (0, 0) [yshift=\l_@@_yshift] grid +(\pagewidth, -\pageheight);
      \end{tikzpicture}
    }
  }
}
%    \end{macrocode}
% \end{macro}

% \section{Blocks}
%
% \subsection{Parameters of Generic \env{list} Environment}
%
% \begin{ldimen}{\leftmargin, \leftmargini}
%    Align lists with the surrounding paragraph indent.
%    \begin{macrocode}
\dim_set_eq:NN \leftmargin \parindent
\dim_set_eq:NN \leftmargini \parindent
%    \end{macrocode}
% \end{ldimen}

% \begin{lmacro}{\@listi, \@listI}
%    Don't put any stretchable space between list elements,
%    and set \cs{topsep} to half a \cs{baselineskip},
%    so that the text will snap back to baseline after the end of the list.
%    \begin{macrocode}
\def\@listi{
  \dim_set_eq:NN \leftmargin \leftmargini
  \skip_set:Nn \topsep {0.5\g_@@_normal_leading}
  \skip_set:Nn \parsep {0pt}
  \skip_set:Nn \itemsep {0pt}
}

\let\@listI\@listi

\@listi
%    \end{macrocode}
% \end{lmacro}

% \subsection{Block Environments Based on \env{list}}
%
% \begin{environment}{quotation, quote}
%    Define environments for setting block quotations.
%    Define both \env{quotation} and \env{quote},
%    as in the standard classes,
%    in case any packages expect one or the other.
%    \begin{macrocode}
\NewDocumentEnvironment{quotation}{}{
  \list{}{
    \dim_set:Nn \itemindent {0pt}
    \dim_set_eq:NN \listparindent \parindent
    \dim_set_eq:NN \rightmargin \leftmargin
  }
  \item\relax
}{
  \endlist
}

\NewEnvironmentCopy{quote}{quotation}
%    \end{macrocode}
% \end{environment}

% \section{Headings \& Subheads}
%
% \begin{imacro}{
%   \section,
%   \subsection,
%   \subsubsection,
%   \paragraph,
%   \subparagraph
% }
%    Define all of the headings and subheads defined in the standard classes.
%    \begin{macrocode}
\newcommand\section{
  \@startsection{section}{1}
  {0pt}
  {
    -\fp_to_dim:n {
      (3\g_@@_normal_leading - \g_@@_large_leading) / 2.701 * 1.701
    }
  }
  {
    \fp_to_dim:n {
      (3\g_@@_normal_leading - \g_@@_large_leading) / 2.701 * 1
    }
  }
  {
    \savepos\RecordProperties{rh@ypos@\thesection}{ypos}
    \headfamily\scshape\large
  }
}

\newcommand\subsection{
  \@startsection{subsection}{2}
  {0pt}
  {
    -\fp_to_dim:n {
      (2\g_@@_normal_leading - \g_@@_normal_leading) / 2.701 * 1.701
    }
  }
  {
    \fp_to_dim:n {
      (2\g_@@_normal_leading - \g_@@_normal_leading) / 2.701 * 1
    }
  }
  {
    \savepos\RecordProperties{rh@ypos@\thesubsection}{ypos}
    \rmfamily\scshape\normalsize
  }
}

\newcommand\subsubsection{
  \@startsection{subsubsection}{3}
  {0pt}
  {
    -\fp_to_dim:n {
      (2\g_@@_normal_leading - \g_@@_normal_leading) / 2.701 * 1.701
    }
  }
  {
    \fp_to_dim:n {
      (2\g_@@_normal_leading - \g_@@_normal_leading) / 2.701 * 1
    }
  }
  {
    \savepos\RecordProperties{rh@ypos@\thesubsubsection}{ypos}
    \rmfamily\itshape\normalsize
  }
}

\newcommand\paragraph{
  \@startsection{paragraph}{4}
  {0pt}
  {\g_@@_normal_leading}
  {-1em}
  {\rmfamily\bfseries\normalsize}
}

\newcommand\subparagraph{
  \@startsection{subparagraph}{5}
  {0pt}
  {\g_@@_normal_leading}
  {-1em}
  {\rmfamily\itshape\normalsize}
}
%    \end{macrocode}
% \end{imacro}

% \begin{lcounter}{secnumdepth}
%    Number all headings down to \cs{subsubsection}.
%    \begin{macrocode}
\setcounter{secnumdepth}{3}
%    \end{macrocode}
% \end{lcounter}

% \begin{lcounter}{
%   section,
%   subsection,
%   subsubsection,
%   paragraph,
%   subparagraph,
% }
%    Define counters used for section numbers.
%    \begin{macrocode}
\newcounter{section}
\newcounter{subsection}[section]
\newcounter{subsubsection}[subsection]
\newcounter{paragraph}[subsubsection]
\newcounter{subparagraph}[paragraph]
%    \end{macrocode}
% \end{lcounter}

% \begin{imacro}{
%   \thesection,
%   \thesubsection,
%   \thesubsubsection,
%   \theparagraph,
%   \thesubparagraph
% }
%    Format section numbers.
%    \begin{macrocode}
\renewcommand\thesection{\arabic{section}}
\renewcommand\thesubsection{\thesection.\arabic{subsection}}
\renewcommand\thesubsubsection{\thesubsection.\arabic{subsubsection}}
\renewcommand\theparagraph{\thesubsubsection.\arabic{paragraph}}
\renewcommand\thesubparagraph{\theparagraph.\arabic{subparagraph}}

\def\@seccntformat#1{\normalfont\csname the#1\endcsname\quad}
%    \end{macrocode}
% \end{imacro}

% If a display heading or subhead is on the first line of the page,
% shift the heading or subhead down from the first baseline
% so that the subsequent text snaps to the baseline grid.
%    \begin{macrocode}
\hook_gput_code:nnn {cmd/section/before}{.}{
  % The section counter hasn't been incremented yet. Temporarily increment in
  % order to reference the position of the right section.
  \addtocounter{section}{1}
  \@@_shift_if_at_top:n {section}{18pt}
  \addtocounter{section}{-1}
}

\hook_gput_code:nnn {cmd/subsection/before}{.}{
  % The subsection counter hasn't been incremented yet. Temporarily increment
  % in order to reference the position of the right section.
  \addtocounter{subsection}{1}
  \@@_shift_if_at_top:n {subsection}{18pt}
  \addtocounter{subsection}{-1}
}

\hook_gput_code:nnn {cmd/subsubsection/before}{.}{
  % The subsubsection counter hasn't been incremented yet. Temporarily
  % increment in order to reference the position of the right section.
  \addtocounter{subsubsection}{1}
  \@@_shift_if_at_top:n {subsubsection}{18pt}
  \addtocounter{subsubsection}{-1}
}
%    \end{macrocode}

% Get the y-coordinate of the top of the text block.
%    \begin{macrocode}
\hook_gput_code:nnn {begindocument/end}{.}{
  \savepos\RecordProperties{rh@ypos@topofpage}{ypos}
}
%    \end{macrocode}

% Define the macro that is expanded in the \hook{cmd/section/before} hook.
% It accepts the \meta{csname} of the heading
% plus the \meta{skip} by which to shift the header
% as arguments.
%
% The macro works by inserting a dimensionless rule
% (before the heading,
% since the macro is expanded in the \hook{cmd/section/before} hook),
% in order to make it possible to insert glue
% between the heading and the top of the page.
% As a consequence of this,
% \cs{topskip} is inserted;
% neutralize this effect by inserting a negative vertical skip
% equal to \cs{topskip}.
% Finally,
% add an additional vertical skip
% specified by the argument \meta{skip}.
%    \begin{macrocode}
\int_new:N \l_@@_ypos_top
\int_new:N \l_@@_ypos_heading

\cs_new:Npn \@@_shift_if_at_top:n #1#2 {
  \RefUndefinedWarn{rh@ypos@topofpage}{ypos}
  \RefUndefinedWarn{rh@ypos@\cs:w the #1\cs_end:}{ypos}

  \int_set:Nn \l_@@_ypos_top {
    \RefProperty{rh@ypos@topofpage}{ypos}
  }
  \int_set:Nn \l_@@_ypos_heading {
    \RefProperty{rh@ypos@\cs:w the #1\cs_end:}{ypos}
  }

  % If zero, the property is undefined. Don't try to shift until the second
  % LaTeX run, when the properties are defined in the .aux file.
  \int_if_zero:nF {\l_@@_ypos_top} {
    \bool_if:nT {
      \int_compare_p:nNn {\l_@@_ypos_heading} = {\l_@@_ypos_top}
      || \int_compare_p:nNn {\l_@@_ypos_heading} > {\l_@@_ypos_top}
    }{
      % Make sure the following is not added to end of previous page
      \penalty -10000\relax
      \rule{0pt}{0pt}
      \skip_vertical:n {-\topskip}
      \skip_vertical:n {-#2}
      \penalty 10000\relax
    }
  }
}
%    \end{macrocode}
%
% \section{Footnotes}
%
% \begin{variable}{\g_@@_footnote_size, \g_@@_footnote_leading}
%    Dimensions to be used by \cs{footnotesize}.
%    \begin{macrocode}
\dim_new:N \g_@@_footnote_size
\dim_new:N \g_@@_footnote_leading

\dim_set:Nn \g_@@_footnote_size {10pt}
\dim_set:Nn \g_@@_footnote_leading {12pt}
%    \end{macrocode}
% \end{variable}

% \begin{imacro}{\footnotesize}
%    Sets the font size and leading in footnotes.
%    Also sets \cs{parskip} equal to one lead,
%    and sets generic list parameters.
%    \begin{macrocode}
\NewDocumentCommand{\footnotesize}{}{
  \@setfontsize\footnotesize{\g_@@_footnote_size}{\g_@@_footnote_leading}
  \dim_set_eq:NN \parindent \baselineskip
  \def\@listi{
    \dim_set_eq:NN \leftmargin \leftmargini
    \skip:set:Nn \topsep {0.5\g_@@_footnote_leading}
    \skip:set:Nn \parsep {0pt}
    \skip:set:Nn \itemsep {0pt}
  }
}
%    \end{macrocode}
% \end{imacro}
%
% \begin{lmacro}{\@makefnmark}
%    Generates the footnote mark.
%    Use superior figures rather than superscript.
%    \begin{macrocode}
\renewcommand\@makefnmark{
  \hbox:n {\normalfont\sufigures\@thefnmark}
}
%    \end{macrocode}
% \end{lmacro}

% \begin{lmacro}{\@makefntext}
%    \marg{text}\\
%    Called by \LaTeX{}'s \cs{footnote} and friends
%    to typeset the actual footnote.
%    Typesets the footnote label one \cs{parindent} into the left margin.
%
%    \cs{parskip} is set to \num{0}\unit{pt}
%    by \cs{@parboxrestore},
%    which is invoked before \cs{@makefntext} in \cs{@footnotetext},
%    so place the footnote mark in a bos of size \cs{baselineskip},
%    which is what \cs{parindent} is supposed to be.
%    \begin{macrocode}
\newcommand\@makefntext[1]{
  % Enter horizontal mode, so that a new paragraph isn't created between the
  % marker and the footnote text
  \noindent
  \hbox_overlap_left:n {
    \hbox_to_wd:nn {\g_@@_footnote_leading}{\@thefnmark\hss}
  }#1
}
%    \end{macrocode}
% \end{lmacro}
%
% \begin{imacro}{\footnoterule}
%    Suppress the footnote rule.
%    \begin{macrocode}
\renewcommand\footnoterule{}
%    \end{macrocode}
% \end{imacro}
%
% \begin{ldimen}{\footnotesep}
%    Affects the height of the box containing the footnote text
%    by setting the height of a strut
%    placed at the beginning of every footnote.
%    The depth of the box containing the footnote text
%    is determined by the depth of a strut
%    placed at the end of every footnote,
%    which is \num{0.3}\cs{baselineskip}.
%    To get a total height of \cs{baselineskip},
%    the first strut needs to have a height of \num{0.7}\cs{baselineskip}.
%    If the height of the first strut
%    is less than \num{0.7}\cs{baselineskip},
%    there is less lead than normal between footnotes
%    (or between the footnote rule and the first footnote).
%    If the height of the first strut
%    is more than \num{0.7}\cs{baselineskip},
%    There will be more lead than normal between footnotes
%    (or between the footnote rule and the first footnote).
%    Set \cs{footnotesep} to \num{0.7}\cs{baselineskip}
%    for uniform leading throughout the footnote block.
%    \begin{macrocode}
\dim_set:Nn \footnotesep {0.7\g_@@_footnote_leading}
%    \end{macrocode}
% \end{ldimen}
%
% \begin{ldimen}{\skip\footins}
%    Determines the distance between the main text
%    and the start of the footnotes.
%    Requires at least one full \cs{baselineskip}
%    between the main text and the start of the footnotes
%    (15pt + 3pt to align the footnote text,
%    which has a leading of 12pt,
%    with the bottom of the text block).
%    \begin{macrocode}
\skip_set:Nn {\skip\footins} {
  \skip_eval:n {
    \g_@@_normal_leading + (\g_@@_normal_leading - \g_@@_footnote_leading)
  }
  plus \g_@@_normal_leading
}
%    \end{macrocode}
% \end{ldimen}

% \section{Load Packages}
%
%    \begin{macrocode}
\RequirePackage{fontspec}
%    \end{macrocode}
%
% Abstract from \LaTeX{}'s counterintuitive layout parameters
% with the \pkg{geometry} package.
%    \begin{macrocode}
\RequirePackage[letterpaper]{geometry}
%    \end{macrocode}

% \section{Font}
%
% Set \acro{EB} Garamond as the default Roman font.
%    \begin{macrocode}
\setmainfont[
  Extension = .otf,
  UprightFont = *-Regular,
  ItalicFont = *-Italic,
  BoldFont = *-SemiBold,
  BoldItalicFont = *-SemiBoldItalic,
%    \end{macrocode}
%
% Use text figures by default.
%    \begin{macrocode}
  Numbers = {Proportional, OldStyle},
%    \end{macrocode}
%
% Set up Swash font.
%    \begin{macrocode}
  FontFace = {m}{sw}{Font=*-Italic, Style=Swash},
  FontFace = {b}{sw}{Font=*-SemiBoldItalic, Style=Swash},
  FontFace = {m}{scsw}{Font=*-Italic, Letters=SmallCaps, Style=Swash},
%    \end{macrocode}
%
% Letterspace small caps,
% and use small caps even for uppercase letters.
%    \begin{macrocode}
  SmallCapsFeatures = {Letters=UppercaseSmallCaps, LetterSpace=4.0},
]{EBGaramond}
%    \end{macrocode}

% \begin{imacro}{\headfamily}
%    Use Cormorant Garamond,
%    which is lighter than \acro{EB} Garamond,
%    as a display font.
%    \begin{macrocode}
\newfontfamily{\headfamily}{CormorantGaramond}[
  SmallCapsFeatures = {Letters=UppercaseSmallCaps, LetterSpace=4.0},
]
%    \end{macrocode}
% \end{imacro}

% \begin{imacro}{\initfamily, \textinit}
%    Switches to \acro{EB} Garamond's font face
%    with decorative initials.
%    \begin{macrocode}
\newfontface\initfamily{EBGaramond-Initials}
\DeclareTextFontCommand{\textinit}{\initfamily}
%    \end{macrocode}
% \end{imacro}

% \begin{imacro}{
%   \textfigures,
%   \liningfigures,
%   \tabularfigures,
%   \proportionalfigures
% }
%    Switch between text figures and lining figures,
%    tabular figures and proportional figures.
%    \begin{macrocode}
\DeclareTextFontCommand{\textfigures}{
  \addfontfeatures{Numbers=OldStyle}
}
\DeclareTextFontCommand{\liningfigures}{
  \addfontfeatures{Numbers=Lining}
}
\DeclareTextFontCommand{\tabularfigures}{
  \addfontfeatures{Numbers=Tabular}
}
\DeclareTextFontCommand{\proportionalfigures}{
  \addfontfeatures{Numbers=Proportional}
}
%    \end{macrocode}
% \end{imacro}

% \begin{imacro}{\sufigures, \textsu}
%    Typeset superior glyphs.
%    \begin{macrocode}
\newcommand\sufigures{\addfontfeatures{VerticalPosition=Superior}}
\DeclareTextFontCommand{\textsu}{\sufigures}
%    \end{macrocode}
% \end{imacro}

% \begin{imacro}{\infigures, \textin}
%    Typeset inferior glyphs.
%    \begin{macrocode}
\newcommand\infigures{\addfontfeatures{VerticalPosition=ScientificInferior}}
\DeclareTextFontCommand{\textin}{\infigures}
%    \end{macrocode}
% \end{imacro}

% \begin{imacro}{\textsw}
%    \marg{text}\\
%    Typeset \meta{text} in Swash shape.
%    \begin{macrocode}
\DeclareTextFontCommand{\textsw}{\swshape}
%    \end{macrocode}
% \end{imacro}

% \begin{imacro}{\MakeUppercase}
%    Letterspace capitals.
%    \begin{macrocode}
\NewCommandCopy\MakeUppercaseCopy\MakeUppercase
\RenewDocumentCommand{\MakeUppercase}{m}{
  \group_begin:
    \addfontfeature{LetterSpace=9.0}\MakeUppercaseCopy{#1}
  \group_end:
}
%    \end{macrocode}
% \end{imacro}

% \section{The Page}
%
% \subsection{Text Block}
%
% \begin{imacro}{\SetTextheight}
%    \marg{ratio}\\
%    Set \(\sfrac{\cs{textheight}}{\cs{textwidth}}\)
%    to be equal to \meta{ratio}.
%    \begin{macrocode}
\NewDocumentCommand{\SetTextheight}{m}{\@@_set_textheight:n {#1}}

\int_new:N \l_@@_num_lines
\dim_new:N \l_@@_tmp

\cs_new:Npn \@@_set_textheight:n #1 {
  \int_set:Nn \l_@@_num_lines {
    \fp_to_int:n {
      (#1\textwidth - \topskip) / \baselineskip
    }
  }
  \geometry{
    textheight=\dim_eval:n {
      \topskip + \l_@@_num_lines\baselineskip
    }
  }
}
%    \end{macrocode}
% \end{imacro}

% Choose a proportion between text height and width
% that appealingly contrasts with the letter page height and width.
% A ratio of \num{1.701}
% (tall pentagon; see \autocite[150]{bringhurst1992ElementsTypographic})
% seems to work well.
%    \begin{macrocode}
\SetTextwidth{70}
\SetTextheight{1.701}
%    \end{macrocode}
%
% Set the bottom margin equal to the horizontal margin.
% This creates an appealing and undramatic
% approximately \num{2:3} ratio
% between the top and bottom margins,
% at least when the line accommodates 70 characters on average.
%    \begin{macrocode}
\geometry{
  hcentering,
  bmargin=\dim_eval:n {(\paperwidth - \textwidth) / 2}
}
%    \end{macrocode}

% \subsection{Page Numbering}
%
%    \begin{macrocode}
\geometry{footskip=3\baselineskip}
\pagenumbering{arabic}
\pagestyle{plain}
%    \end{macrocode}
%
% \end{implementation}
%
% \printbibliography
